import { CustomContentDirective } from './custom-content.directive';

describe('CustomContentDirective', () => {
  it('should create an instance', () => {
    const directive = new CustomContentDirective();
    expect(directive).toBeTruthy();
  });
});
